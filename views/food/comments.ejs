<h1>Comments</h1>
<!-- <% comment.forEach(element => { %>
    <p><%= element.comment %></p>
    <p><%= element.userId %></p>
    <form action="/search/<%=element.recipeId%>/edit" method="GET">
        <input type="submit" value="Edit" />
      </form>
    <form action="/search/<%=element.id%>?_method=DELETE" method="POST">
        <input type="submit" value="Delete" />
      </form>
<% }) %>  -->


<!-- Why is this not working? Only want to able to display edit/delete for the user's comment not others -->
<% comment.forEach(element => { %>
  <p><%= element.comment %></p>
  <p><%= element.userId %> </p>
  <% if (element.userId == user.id) { %>
    <form action="/search/<%=element.recipeId%>/edit" method="GET">
      <input type="submit" value="edit" />
    </form>
  <form action="/search/<%=element.id%>/?_method=DELETE" method="POST">
      <input type="submit" value="delete" />
    </form>
  <% } else { %>
  <% } %>
<% }) %>